{
  "framework": "nextjs",
  "buildCommand": "pnpm install --no-frozen-lockfile && pnpm turbo build --filter=@exploremy/web",
  "outputDirectory": ".next",
  "installCommand": "npm install -g pnpm@9 && pnpm install --no-frozen-lockfile"
}
